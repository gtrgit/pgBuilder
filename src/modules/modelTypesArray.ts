 import {default as modelTypes} from "../modelTypeColour.json"
 //import modelTypes from "modelTypeColour"

for (const key in modelTypes.models) {
    if (Object.prototype.hasOwnProperty.call(modelTypes.models, key)) {
        const element = modelTypes.models[key];
        
    }
}